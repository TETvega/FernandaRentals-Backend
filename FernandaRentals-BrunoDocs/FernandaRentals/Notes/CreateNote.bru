meta {
  name: CreateNote
  type: http
  seq: 4
}

post {
  url: {{PATH}}/notes
  body: json
  auth: bearer
}

auth:bearer {
  token: {{TOKEN}}
}

body:json {
      {
        "name": "{{NAME}}",
        "eventId": "{{EVENT_ID}}",
        "description": "{{DESCRIPTION}}"
      }
}

vars:pre-request {
  NAME: Hola, soy el admin ANNER
  EVENT_ID: 7928977d-4f38-4e3d-64a1-08dd174c60e1
  DESCRIPTION: a verrr
}
